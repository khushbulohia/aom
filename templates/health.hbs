{{#if health}}
	>>>Here is a snapshot of your applications health at *_{{nspath}}_*\n
	{{#if health.good}}
		:white_check_mark: {{health.good}} (good)\n
	{{/if}}
	{{#if health.notify}}
		:warning: {{health.notify}} (notify)\n
	{{/if}}
	{{#if health.unhealthy}}
		:x: {{health.unhealthy}} (not healthy)\n
	{{/if}}
{{else}}
	Didn't find health for the application at {{nspath}}.
{{/if}}